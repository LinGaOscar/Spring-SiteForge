spring:
  application:
    name: portal-cms
  jpa:
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8200
  servlet:
    context-path: /cws

storage:
  upload-dir: ./uploads
