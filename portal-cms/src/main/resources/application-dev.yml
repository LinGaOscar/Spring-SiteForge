spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/siteforge_db
    username: siteforge
    password: siteforge
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

cms:
  preview-base-url: http://localhost:8100
